@using Microsoft.AspNetCore.Identity
@using Candyshop.Models

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager

@if (SignInManager.IsSignedIn(User) && await UserManager.IsInRoleAsync(UserManager.GetUserAsync(User).Result, "Admin"))
{
   <li class="dropdown">
    <a class="dropdown-toggle" href="#" data-toggle="dropdown">Admin</a>
    <ul class="dropdown-menu">
        <li>
            <a class="dropdown-item" asp-area="Admin" asp-controller="Admin" asp-action="Index">Candy</a>
            <a class="dropdown-item" asp-area="Admin" asp-controller="Categories" asp-action="Index">Catagory</a>
        </li>
    </ul>
   </li>
}

